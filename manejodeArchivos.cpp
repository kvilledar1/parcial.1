#include "archivo.h"
#include "iostream"
#include "fstream"
using namespace std;
string texto;

void numero (string direnctorio){
    ifstream  numero;
    numero.open(directorio);
    if(numero.is_open()){
        while (getline(numero,texto)){
            cout
        }
    }
}